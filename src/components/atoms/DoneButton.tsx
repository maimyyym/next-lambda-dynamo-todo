import React from "react";

export type DoneButtonProps = {

};

export const DoneButton: React.VFC<DoneButtonProps> = () => {
    return (
        <button className="w-3 h-3 rounded-full bg-darkGray" />
    );
};

export default DoneButton;
